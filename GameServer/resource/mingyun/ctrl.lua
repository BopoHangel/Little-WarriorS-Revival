function config(map)
    MapCanSavePos(map, 1) --Set whether or not to preserve the role of location map (map, coordinates and direction), this setting affect all copies of the map
  --  MapCanPK(map, 1) --Can map settings PK, this setting affect all copies of the map
  --  MapCopyNum(map, 1) --Set the number of copies of the map, if we do not call the statement, use the default value 1
  --  SingleMapCopyPlyNum(map, 300)
  --  MapCanTeam(map , 1)
 --   MapType ( map , 4 )

end

function init_entry(map)

end
